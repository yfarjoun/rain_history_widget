# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — it contains only `README.md` with the project name (`rain_history_widget`). There is no source code, build system, dependency manifest, test suite, or linter configured yet.

When the project is scaffolded, update this file with:
- Build / run / test / lint commands (including how to run a single test)
- High-level architecture once multiple files exist and the structure isn't obvious from a quick directory scan

## Branch convention

Active development branch for the initial setup: `claude/init-project-setup-Izazg`. Commit and push work there rather than to `main`/`master`.
